# SPDX-License-Identifier: Apache-2.0
#
# M5 content-core sim gate: the launcher over a ram-disk FAT seeded with
# a content core (entry 0) + a WAD in /RAM:/roms, driven by the
# content-gate timeline (launch core 0 -> browse -> pick WAD 0 -> read +
# render). Build:
#   RETRO_CORE_LLEXT=<content.llext> RETRO_CORE_LLEXT2=<2048.llext> \
#   RETRO_SEED_WAD=<some.wad> \
#   west build -b qemu_cortex_a53 -s apps/retro -- \
#     -DEXTRA_CONF_FILE=overlays/qemu_content.conf \
#     -DEXTRA_DTC_OVERLAY_FILE=overlays/qemu_ramdisk.overlay

CONFIG_RETRO_CORE_LLEXT_FS=y
CONFIG_RETRO_MENU=y
CONFIG_RETRO_MENU_DIR="/RAM:/cores"
CONFIG_RETRO_CONTENT_DIR="/RAM:/roms"
CONFIG_RETRO_STORAGE_MOUNT="/RAM:"
CONFIG_RETRO_MENU_WIDTH=640
CONFIG_RETRO_MENU_HEIGHT=480

# Ram-disk FAT (zephyr,ram-disk "RAM" in the overlay), formatted on
# first mount, then seeded by retro_qemu_seed.c (cores + a WAD).
CONFIG_DISK_ACCESS=y
CONFIG_DISK_DRIVERS=y
CONFIG_DISK_DRIVER_RAM=y
CONFIG_FILE_SYSTEM=y
CONFIG_FAT_FILESYSTEM_ELM=y
CONFIG_FS_FATFS_LFN=y
CONFIG_FS_FATFS_LFN_MODE_STACK=y
CONFIG_FILE_SYSTEM_MKFS=y
CONFIG_FS_FATFS_MOUNT_MKFS=y
CONFIG_RETRO_QEMU_RAMDISK_SEED=y

# The content-gate timeline drives launch + content pick.
CONFIG_RETRO_SCRIPTED_CONTENT=y
CONFIG_RETRO_CAPTURE_EVERY=30
