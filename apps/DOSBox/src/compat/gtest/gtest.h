/* Empty gtest stub — shell.cpp includes <gtest/gtest.h> unconditionally
 * but uses no test macros; the vendored googletest in dosbox-x/include
 * drags in POSIX surface picolibc doesn't have.
 */
#ifndef PIZZA_COMPAT_GTEST_H
#define PIZZA_COMPAT_GTEST_H
#endif
