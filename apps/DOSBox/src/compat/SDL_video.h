/* Compat SDL_video.h — some DOSBox-X TUs include it directly; the
 * shim's monolithic SDL.h already declares the video surface API.
 */
#ifndef PIZZA_COMPAT_SDL_VIDEO_H
#define PIZZA_COMPAT_SDL_VIDEO_H
#include "SDL.h"
#endif
